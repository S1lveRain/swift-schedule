import SwiftUI

struct ScheduleView: View {
    var body: some View {
        VStack {
            Text("Расписание")
                .padding()
        }
    }
}

struct scheduleView_Previews: PreviewProvider {
    static var previews: some View {
        ScheduleView()
    }
}
